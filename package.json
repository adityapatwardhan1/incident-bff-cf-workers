{
  "name": "incident-bff-cf-workers",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:phase-0": "vitest run tests/phase-0",
    "test:phase-1": "vitest run tests/phase-1",
    "test:phase-2": "vitest run tests/phase-2",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.8.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.0.0"
  }
}
